services:
  ai-service:
    build: ./ai-service
    ports:
      - "127.0.0.1:8001:8000"
    volumes:
      - ./ai-service:/app 
    environment:
      - ENV=development

#  backend:
#    build: ./backend
#    ports:
#      - "8000:8000"
#    depends_on:
#      - ai-service

#  frontend:
#    build: ./frontend
#    ports:
#      - "3000:3000"
#    depends_on:
#      - backend
